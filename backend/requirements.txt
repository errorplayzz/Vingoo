# ─────────────────────────────────────────────────────────────────────────────
# Money Muling Detection System – Python dependencies
# RIFT 2026 Hackathon
#
# Install with:   pip install -r requirements.txt
# Run with:       uvicorn main:app --reload --host 0.0.0.0 --port 8000
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.129.0

# ASGI server (with websockets, watchfiles for --reload)
uvicorn[standard]>=0.29.0

# Data handling (3.x wheel available for Python 3.14)
pandas>=3.0.0

# Graph algorithms (length_bound in simple_cycles requires >= 3.2)
networkx>=3.3

# Pydantic v2
pydantic>=2.7.0

# File upload support (used by FastAPI for UploadFile)
python-multipart>=0.0.9

# ── Database persistence (Phase 6) ───────────────────────────────────────────

# SQLAlchemy ORM + core
sqlalchemy>=2.0.0

# PostgreSQL driver (binary wheel — no libpq compile needed)
psycopg2-binary>=2.9.9

# Load .env files automatically
python-dotenv>=1.0.0

# Async HTTP client for AI explanation layer (OpenRouter) + HTTP/2 support
httpx[http2]>=0.27.0

# High-performance event loop (Linux/macOS only – auto-skipped on Windows)
uvloop>=0.19.0 ; sys_platform != "win32"

# Redis async client + hiredis C-extension parser (2–3× faster deserialisation)
# hiredis extra installs the C parser automatically when available
redis[hiredis]>=5.0.0

# ── ML predictive layer ───────────────────────────────────────────────────────

# scikit-learn: IsolationForest, RobustScaler, Pipeline
# joblib is bundled with scikit-learn and used for model persistence
scikit-learn>=1.4.0

# NumPy: feature matrix operations in ml_model.py
# (also a transitive dep of pandas/networkx – pinned here for explicitness)
numpy>=1.26.0
